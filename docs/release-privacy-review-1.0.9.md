# 1.0.9 发布审查

## 范围与需求

- 基线：GitHub `main` 的 `31dc4bd`（1.0.5）。功能差异固定为 `git diff 31dc4bd...341edc2`；后续仅调整 1.0.9 / build 8 版本元数据、发布与第三方组件说明。
- 需求来自本次对话：mpv 内置播放与硬解、修复稍后自动闪退，以及“review 一下 code 确认没有泄露隐私就也上传到 GitHub，版本号就 1.0.9”。
- 检查发布源码树与历史、打包脚本、mpv 请求头和错误输出、CoreAudio 回移、配置/缓存/实测记录的存储位置、最终 DMG 内应用及扩展属性。
- 两个独立审查代理因额度限制未能执行；以下两个维度均由当前代理完成，不记为独立交叉审查。

## Standards

新增发现：0；无发布阻断项。

- `project.yml` 与生成的 Xcode 工程一致，两个目标均为 1.0.9 / build 8。
- CoreAudio 回移保留原始许可、固定版本头文件和来源校验值；仅 macOS 编译，内部 ABI 依赖及升级移除步骤已记录。
- 源码不跟踪本机配置、证书、数据库或构建产物。打包执行空预设检查、调试路径清理、重新签名和应用包隐私审计。
- 仅发布以现有公开 `main` 为父提交的源码快照，避免携带本地开发历史；提交身份使用现有公开 GitHub noreply 邮箱。无关未跟踪文件不进入发布树。

## Spec

新增发现：0；在检查范围内未发现本机隐私夹带。

- 最终安装包的接口预设为 `[]`，不包含本机接口配置、播放历史、收藏、账户数据库或诊断日志。
- 未发现本机主目录/工作区路径、个人签名身份、密钥正文或常见令牌格式；扩展属性亦已检查。三方依赖自身的公开构建路径和解析常量不视为本机数据。
- mpv 请求头仅传给播放器，不写入新增日志；源检测持久化记录使用散列身份，运行时私有文件位于应用外的用户目录，不由发布脚本收集。
- CoreAudio 初始化失败清理回移保留；没有通过关闭 mpv 或硬解来规避崩溃。

## 验证与限制

- 同一功能代码上一轮 macOS XCTest：195 项通过，0 失败，包括闪退复现回归和实际 VideoToolbox 硬解。本轮仅修改版本与文档，未重复执行该全量测试。
- 本轮 7 项发布隐私回归测试通过；1.0.9 Universal 2 Release 构建、签名、DMG 校验及挂载后的应用隐私审计通过。
- DMG 根目录仅有 `TVBox.app` 和指向 Applications 的安装快捷方式。应用版本确认是 1.0.9 / build 8。
- 数小时连续播放、真实蓝牙设备切换及 Intel 硬解尚未实机验证。应用使用 ad-hoc 签名，未公证。
- 检查结论仅限上述规则与人工审阅范围，不能保证识别所有未知凭据格式；原始私密匹配值不会写入报告。

DMG SHA-256：`4c9750a15efd0be13020163c21a1429649f96c1ee7d471d95704c46a342a40e1`

发布最后关卡：对隔离的最终公开提交历史再运行审计，并将 GitHub 上的提交、tag 与资产 SHA-256 和本地结果逐一比对。

结论：Standards 0 项新增发现；Spec 0 项新增发现，两轴均无未处理的发布阻断项。
